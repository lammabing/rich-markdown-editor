The task is to create a modern markdown editor with support for the following items/elements. 

Real-time rendering of standard Markdown as well as GFM extensions with support for tables, tasks lists, strikethrough(GFM and CriticMarkup), Insertion, Highlight, Autolinks, Emoji, Math Latex (inline and block equations), Code blocks with syntax highlighting, footnotes, alerts (callouts) and custom HTML code blocks renders

The editor should immediately recognize and render the items in place appropriately. So the rendering takes place right on the editor without a rendering section. 

When the user types slash '/' the commands box should come up yo allow user to type guided command without the need to memorize all the syntax. This should allow users to easily insert elements and input colored text, snippets, etc. 

Interactive checklists using - [x] - Turn notes into action - check tasks off directly in the editor.

Before proceeding with writing the code, first re-organize and rewrite in an logical clear fashion the list of features mentioned. Then proceed to write out a detailed plan including tools, tech stack that will be used to create this editor. Include feasibility analysis, proposed approach to creating functionality. Also include a timeframe.

Do not implement or write any code until above has been documented, reviewed and approved. 